- [x] Create `components/Callout.tsx` with props for text (one line), button text, button URL (internal or external), optional background image URL (defaults to linear-gradient(135deg, var(--color-primary-lighter), var(--color-primary-light)) if not provided), and optional alt text for images; use Next.js Link for internal URLs and anchor tag for external URLs; ensure fallback to default background if image fails
- [x] Add `styles/components/callout.css` with styles for full width layout, height var(--space-7xl) at 1920px baseline (adjusting proportionally on mobile), vertical gap var(--space-lg) between text and button, centered text and button, default background linear-gradient(135deg, var(--color-primary-lighter), var(--color-primary-light)), text styling: color #000, font-size var(--fs-h3), font-style normal, font-weight 600, line-height normal, and button styling: display inline-flex, padding var(--space-sm) var(--space-md), flex-direction column, align-items flex-start, border-radius var(--radius-sm), font-size var(--fs-body) for the label; background styling applied via user image (using Next.js Image for optimization) or default
- [x] Import `callout.css` in `app/globals.css`
- [x] Add TypeScript types in `types/` if needed
- [x] Create unit tests in `__tests__/components/Callout.test.tsx` covering text display, button click, background rendering (default and image), and fallback behavior
- [x] Run `pnpm check` to ensure no linting or type errors
- [x] Test component rendering in a page (e.g., add to home page temporarily for verification); include basic e2e testing for navigation if applicable
