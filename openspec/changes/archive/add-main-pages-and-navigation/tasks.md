````markdown
```markdown
## 1. Implementation
- [ ] 1.1 Create page routes in `app/` for: `/`, `/media-reviews`, `/franchise`, `/careers`, `/contact`, `/terms`, `/privacy`.
- [ ] 1.2 Create `components/Header.tsx` with links to Home, Media Reviews, Franchise, Careers, Contact.
- [ ] 1.3 Create `components/Footer.tsx` with links to Terms & Conditions and Privacy Policy.
- [ ] 1.4 Add header and footer into `app/layout.tsx` so they appear on all public pages.
- [ ] 1.5 Add minimal page content and SEO metadata (`<title>`, `<meta name="description">`) for each page.
- [ ] 1.6 Add unit tests for navigation behavior and render smoke tests for each page.
- [ ] 1.7 Run accessibility checks (axe) on header/footer and landing pages.
- [ ] 1.8 Update `openspec/specs/` if any capabilities become normative beyond this change.
- [ ] 1.9 Run `openspec validate add-main-pages-and-navigation --strict` and resolve any issues.
- [ ] 1.10 Create a PR with changes and request review.
 - [x] 1.1 Create page routes in `app/` for: `/`, `/media-reviews`, `/franchise`, `/careers`, `/contact`, `/terms`, `/privacy`.
 - [x] 1.2 Create `components/Header.tsx` with links to Home, Media Reviews, Franchise, Careers, Contact.
 - [x] 1.3 Create `components/Footer.tsx` with links to Terms & Conditions and Privacy Policy.
 - [x] 1.4 Add header and footer into `app/layout.tsx` so they appear on all public pages.
 - [x] 1.5 Add minimal page content and SEO metadata (`<title>`, `<meta name="description">`) for each page.
 - [x] 1.6 Add unit tests for navigation behavior and render smoke tests for each page.
 - [ ] 1.7 Run accessibility checks (axe) on header/footer and landing pages.
 - [ ] 1.8 Update `openspec/specs/` if any capabilities become normative beyond this change.
 - [ ] 1.9 Run `openspec validate add-main-pages-and-navigation --strict` and resolve any issues.
 - [ ] 1.10 Create a PR with changes and request review.

## Notes
- Keep pages minimal (content placeholders) to verify navigation early; copy-ready content can be added in subsequent iteration.


```

````
